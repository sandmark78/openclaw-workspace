# 2026-08-05 工作日志

## 00:10 UTC - 子Agent架构修复

### 问题
- 素材抓取Cron直接和老大说话（delivery: announce）
- 素材写入固定文件，无法复用
- 文章Cron读固定素材，无法从素材池选题

### 修复
1. **5个素材抓取Cron**：
   - delivery改为none（不和老大说话）
   - 写入topics/pool/YYYY-MM-DD-slot.md
   - prompt精简，不汇报

2. **5个文章Cron**：
   - 从topics/pool/素材池选题
   - 读质量指南（article-quality-guide.md）
   - 验证占位符

3. **素材池结构**：
   - topics/pool/ - 待用素材
   - topics/used/ - 已用素材（文章Cron用完后移过去）

### 架构梳理
- 主Agent（我）：分配任务、审核质量、汇报老大
- 子Agent：执行具体工作，不直接和老大说话
- 素材池：解耦抓取和写作，支持复用

### 文件路径统一
- 全部用 /home/node/.openclaw/workspace/sandbot-blog/
- 不再用 /tmp/sandbot-gh/（符号链接，容易混淆）

## 01:31 UTC - 子Agent分工教训（老大指出）

### 问题
- 对话中老大让我处理问题时，我批量 spawn 子 agent 去做
- 老大看不到素材抓取和文章写作的产出

### 老大的话
> "素材抓取和文章写作要带给我啊！不然我怎么知道"
> "每次让你处理问题的时候你批量调用子agent，这个不好"

### 正确分工
| 场景 | 做法 |
|------|------|
| Cron 素材/文章任务 | isolated session 跑，delivery=announce 推送给老大 |
| 对话中老大让我处理问题 | 我自己直接做，不批量 spawn 子 agent |
| 子 agent 汇报 | 他们做了汇报给我，我汇总后汇报给老大 |

### 修复
- 10个素材/文章 Cron 全部改成 delivery=announce
- 对话中不批量 spawn，自己直接干

### 铁律新增
- **对话中不批量委托子 agent** — 老大找我是要我解决，不是要我当传话筒
- **Cron 产出必须 announce** — 素材抓了什么、文章写了什么，老大要看


## 12:58 UTC - 工作流反思与修复（老大要求）

### 今天的6个问题和根因
1. **Cron简化过头** → 文章来源显示"来源说明"占位符
2. **音频路径写死article.mp3** → 播放器找不到文件
3. **播放器display: none** → 有音频但看不到
4. **SSML标签被读出** → edge-tts-human.py的bug
5. **markdown符号被读出** → extract-article-text.py没过滤
6. **来源缺失** → JSON没填source_note字段

### 根本原因
**工作流缺少端到端验证，每一步都假设上一步做对了。**

### 修复方案
1. **publish-article.sh加验证关卡**：
   - 检查音频路径是否正确（不能是article.mp3）
   - 检查播放器是否显示（不能是display: none）
   - 检查来源是否填写（不能是"来源说明"）

2. **质量指南更新**：
   - 强制要求填source_note字段
   - 明确格式：来源类型+具体信息+数据可靠性

3. **模板同步**：
   - 改了模板要批量更新所有文章
   - 不能只改模板不管现有页面

4. **铁律刻入**：
   - 第28条：对话中不批量spawn子agent
   - 第30条：子agent只用于Cron定时任务

### 以后怎么避免
- **每次发布前跑完整验证**：不能假设上一步做对了
- **改脚本后必须测试**：edge-tts-human.py的bug就是没测试导致的
- **改模板后必须同步**：不能只改模板不管现有文章
- **Cron prompt不能简化过头**：关键步骤不能省

### 已完成的修复
- ✅ edge-tts-human.py：移除SSML，改用纯文本
- ✅ extract-article-text.py：过滤markdown符号
- ✅ 5篇文章音频路径修正
- ✅ 模板去掉会员订阅内容
- ✅ 质量指南更新（要求填source_note）
- ✅ 铁律28/30条刻入MEMORY.md

### 待完成
- ⏳ publish-article.sh加验证关卡（下一步）
- ⏳ 批量更新所有文章去掉订阅内容（进行中）


## 12:58 UTC - 工作流反思与修复（老大要求）

### 今天的6个问题和根因
1. **Cron简化过头** → 文章来源显示"来源说明"占位符
2. **音频路径写死article.mp3** → 播放器找不到文件
3. **播放器display: none** → 有音频但看不到
4. **SSML标签被读出** → edge-tts-human.py的bug
5. **markdown符号被读出** → extract-article-text.py没过滤
6. **来源缺失** → JSON没填source_note字段

### 根本原因
**工作流缺少端到端验证，每一步都假设上一步做对了。**

### 修复方案
1. **publish-article.sh加验证关卡**：
   - 检查音频路径是否正确（不能是article.mp3）
   - 检查播放器是否显示（不能是display: none）
   - 检查来源是否填写（不能是"来源说明"）

2. **质量指南更新**：
   - 强制要求填source_note字段
   - 明确格式：来源类型+具体信息+数据可靠性

3. **模板同步**：
   - 改了模板要批量更新所有文章
   - 不能只改模板不管现有页面

4. **铁律刻入**：
   - 第28条：对话中不批量spawn子agent
   - 第30条：子agent只用于Cron定时任务

### 以后怎么避免
- **每次发布前跑完整验证**：不能假设上一步做对了
- **改脚本后必须测试**：edge-tts-human.py的bug就是没测试导致的
- **改模板后必须同步**：不能只改模板不管现有文章
- **Cron prompt不能简化过头**：关键步骤不能省

### 已完成的修复
- ✅ edge-tts-human.py：移除SSML，改用纯文本
- ✅ extract-article-text.py：过滤markdown符号
- ✅ 5篇文章音频路径修正
- ✅ 模板去掉会员订阅内容
- ✅ 质量指南更新（要求填source_note）
- ✅ 铁律28/30条刻入MEMORY.md

### 待完成
- ⏳ publish-article.sh加验证关卡（下一步）
- ⏳ 批量更新所有文章去掉订阅内容（进行中）


## 12:58 UTC - 工作流反思完成，待执行修复

### 已完成
- ✅ 反思记录写入memory
- ✅ 铁律28/30条刻入MEMORY.md
- ✅ edge-tts-human.py移除SSML
- ✅ extract-article-text.py过滤markdown符号
- ✅ 5篇文章音频路径修正
- ✅ 模板去掉会员订阅
- ✅ 质量指南更新

### 待执行（下一轮继续）
- ⏳ publish-article.sh加3个验证关卡：
  1. 音频路径验证（不能是article.mp3，自动修正）
  2. 播放器显示验证（移除display:none）
  3. 来源验证（警告"来源说明"占位符）
- ⏳ 最终验证增加3项检查：音频路径、播放器可见性、来源完整性
- ⏳ 推送GitHub

### 根本原因
工作流缺少端到端验证，每一步都假设上一步做对了。
修复思路：在publish-article.sh中加入自动修正+验证关卡，发布前自动修复常见问题。


## 13:11 UTC - publish-article.sh 修复内容（待执行）

### 需要添加的3个验证关卡（在占位符检查之后）

**1.5 音频路径验证**
```bash
# 修正错误的音频路径（article.mp3 → 正确文件名）
WRONG_AUDIO=$(grep -c 'src="audio/article.mp3"' "$ARTICLE_FILE" 2>/dev/null || echo "0")
if [ "$WRONG_AUDIO" -gt 0 ]; then
  echo "   ⚠️  修正: audio/article.mp3 → audio/$ARTICLE_BASE.mp3"
  sed -i "s|src=\"audio/article.mp3\"|src=\"audio/$ARTICLE_BASE.mp3\"|g" "$ARTICLE_FILE"
fi
```

**1.6 播放器显示验证**
```bash
# 检查播放器是否被隐藏（模板默认 display: none）
HIDDEN_PLAYER=$(grep -c 'id="audioPlayer" style="display: none;"' "$ARTICLE_FILE" 2>/dev/null || echo "0")
if [ "$HIDDEN_PLAYER" -gt 0 ]; then
  echo "   ⚠️  修正: 播放器 display:none → 移除隐藏样式"
  sed -i 's|id="audioPlayer" style="display: none;"|id="audioPlayer"|g' "$ARTICLE_FILE"
fi
```

**1.7 来源验证**
```bash
# 检查来源是否是占位符
PLACEHOLDER_SOURCE=$(grep -c "来源说明" "$ARTICLE_FILE" 2>/dev/null || echo "0")
if [ "$PLACEHOLDER_SOURCE" -gt 0 ]; then
  echo "   ⚠️  来源显示'来源说明'占位符（建议手动补充，不阻塞发布）"
else
  echo "   ✅ 来源信息完整"
fi
```

### TTS参数修复（第3步语音生成）
```bash
# 修复：添加 -10% 语速参数
python3 "$SCRIPT_DIR/edge-tts-human.py" \
  /tmp/tts-input.txt \
  "$AUDIO_DIR/$ARTICLE_BASE.mp3" \
  zh-CN-YunxiNeural \
  -10%  # ← 添加这个参数
```

### 最终验证增强（第9步）
在现有验证后添加3项检查：
```bash
# 检查音频路径是否正确
CORRECT_AUDIO=$(grep -c "src=\"audio/$ARTICLE_BASE.mp3\"" "$ARTICLE_FILE" 2>/dev/null || echo "0")
if [ "$CORRECT_AUDIO" -gt 0 ]; then
  echo "   ✅ 音频路径正确"
else
  echo "   ⚠️  音频路径可能有问题"
fi

# 检查播放器是否可见
PLAYER_HIDDEN=$(grep -c 'style="display: none;"' "$ARTICLE_FILE" 2>/dev/null || echo "0")
if [ "$PLAYER_HIDDEN" -eq 0 ]; then
  echo "   ✅ 播放器可见"
else
  echo "   ⚠️  播放器可能被隐藏"
fi

# 检查来源是否完整
BAD_SOURCE=$(grep -c "来源说明" "$ARTICLE_FILE" 2>/dev/null || echo "0")
if [ "$BAD_SOURCE" -eq 0 ]; then
  echo "   ✅ 来源信息完整"
else
  echo "   ⚠️  来源显示占位符"
fi
```

### 执行步骤
1. 用write工具重写publish-article.sh（完整内容已准备好）
2. 测试：`./publish-article.sh posts/test.html blog.html`
3. 推送GitHub

### 根本原因总结
工作流缺少端到端验证，每一步都假设上一步做对了。
修复思路：在publish-article.sh中加入自动修正+验证关卡，发布前自动修复常见问题。


## 13:11 UTC - publish-article.sh 修复内容（待执行）

### 需要添加的3个验证关卡（在占位符检查之后）

**1.5 音频路径验证**
```bash
# 修正错误的音频路径（article.mp3 → 正确文件名）
WRONG_AUDIO=$(grep -c 'src="audio/article.mp3"' "$ARTICLE_FILE" 2>/dev/null || echo "0")
if [ "$WRONG_AUDIO" -gt 0 ]; then
  echo "   ⚠️  修正: audio/article.mp3 → audio/$ARTICLE_BASE.mp3"
  sed -i "s|src=\"audio/article.mp3\"|src=\"audio/$ARTICLE_BASE.mp3\"|g" "$ARTICLE_FILE"
fi
```

**1.6 播放器显示验证**
```bash
# 检查播放器是否被隐藏（模板默认 display: none）
HIDDEN_PLAYER=$(grep -c 'id="audioPlayer" style="display: none;"' "$ARTICLE_FILE" 2>/dev/null || echo "0")
if [ "$HIDDEN_PLAYER" -gt 0 ]; then
  echo "   ⚠️  修正: 播放器 display:none → 移除隐藏样式"
  sed -i 's|id="audioPlayer" style="display: none;"|id="audioPlayer"|g' "$ARTICLE_FILE"
fi
```

**1.7 来源验证**
```bash
# 检查来源是否是占位符
PLACEHOLDER_SOURCE=$(grep -c "来源说明" "$ARTICLE_FILE" 2>/dev/null || echo "0")
if [ "$PLACEHOLDER_SOURCE" -gt 0 ]; then
  echo "   ⚠️  来源显示'来源说明'占位符（建议手动补充，不阻塞发布）"
else
  echo "   ✅ 来源信息完整"
fi
```

### TTS参数修复（第3步语音生成）
```bash
# 修复：添加 -10% 语速参数
python3 "$SCRIPT_DIR/edge-tts-human.py" \
  /tmp/tts-input.txt \
  "$AUDIO_DIR/$ARTICLE_BASE.mp3" \
  zh-CN-YunxiNeural \
  -10%  # ← 添加这个参数
```

### 最终验证增强（第9步）
在现有验证后添加3项检查：
```bash
# 检查音频路径是否正确
CORRECT_AUDIO=$(grep -c "src=\"audio/$ARTICLE_BASE.mp3\"" "$ARTICLE_FILE" 2>/dev/null || echo "0")
if [ "$CORRECT_AUDIO" -gt 0 ]; then
  echo "   ✅ 音频路径正确"
else
  echo "   ⚠️  音频路径可能有问题"
fi

# 检查播放器是否可见
PLAYER_HIDDEN=$(grep -c 'style="display: none;"' "$ARTICLE_FILE" 2>/dev/null || echo "0")
if [ "$PLAYER_HIDDEN" -eq 0 ]; then
  echo "   ✅ 播放器可见"
else
  echo "   ⚠️  播放器可能被隐藏"
fi

# 检查来源是否完整
BAD_SOURCE=$(grep -c "来源说明" "$ARTICLE_FILE" 2>/dev/null || echo "0")
if [ "$BAD_SOURCE" -eq 0 ]; then
  echo "   ✅ 来源信息完整"
else
  echo "   ⚠️  来源显示占位符"
fi
```

### 执行步骤
1. 用write工具重写publish-article.sh（完整内容已准备好）
2. 测试：`./publish-article.sh posts/test.html blog.html`
3. 推送GitHub

### 根本原因总结
工作流缺少端到端验证，每一步都假设上一步做对了。
修复思路：在publish-article.sh中加入自动修正+验证关卡，发布前自动修复常见问题。


## 13:48 UTC - 工作流修复完成总结

### 已完成的修复

**1. publish-article.sh 增强（2026-08-05）**
- ✅ 添加音频路径验证（自动修正 article.mp3 → 正确文件名）
- ✅ 添加播放器显示验证（自动移除 display: none）
- ✅ 添加来源验证（警告"来源说明"占位符）
- ✅ TTS参数修复（添加 -10% 语速）
- ✅ 最终验证增强（3项新检查）

**2. 批量修复所有文章**
- ✅ 播放器隐藏：0篇（修复9篇）
- ✅ 来源占位符：0篇（修复7篇）
- ✅ 音频路径错误：0篇
- ✅ Git推送：commit b9ade0a

**3. 脚本修复**
- ✅ edge-tts-human.py：移除SSML，改用纯文本TTS
- ✅ extract-article-text.py：过滤markdown符号（**、*、`等）
- ✅ 模板更新：去掉会员订阅内容

**4. 铁律刻入MEMORY.md**
- ✅ 第28条：对话中不批量spawn子agent
- ✅ 第30条：子agent只用于Cron定时任务

### 根本原因总结
工作流缺少端到端验证，每一步都假设上一步做对了。
修复思路：在publish-article.sh中加入自动修正+验证关卡，发布前自动修复常见问题。

### 以后怎么避免
- ✅ 每次发布前跑完整验证（不能假设上一步做对了）
- ✅ 改脚本后必须测试（edge-tts-human.py的bug就是没测试导致的）
- ✅ 改模板后必须同步（不能只改模板不管现有文章）
- ✅ Cron prompt不能简化过头（关键步骤不能省）

### 待观察
- 下次Cron生成文章时，验证publish-article.sh的3个关卡是否生效
- 观察音频质量（纯文本TTS vs SSML）
- 检查来源信息是否完整（Cron prompt已更新质量指南）
