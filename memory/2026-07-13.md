# 2026-07-13 每日记录

## 教训：AI Agent 不会忘记，但必须写入工作流文件

**问题**: Cron 发布文章后只输出相对路径，老大无法直接访问
**根因**: 没有在工作流文件中明确要求输出完整URL
**教训**: 
- "AI agent不会忘记，但必须写入工作流文件"
- "发布文章 = 写HTML + 更新blog.html + git push + 输出完整URL"

**已更新的文件**:
1. ✅ MEMORY.md - 加入"发布文章必须输出完整URL"教训
2. ✅ AGENTS.md - 加入"发布工作流"章节
3. ✅ publish-article.sh - 自动输出完整URL

**验证方法**:
```bash
# 检查 publish-article.sh 是否包含 URL 输出
tail -10 /tmp/sandbot-gh/scripts/publish-article.sh

# 检查 AGENTS.md 是否包含发布工作流
grep -A 10 "发布工作流" /home/node/.openclaw/workspace/AGENTS.md

# 检查 MEMORY.md 是否包含教训
grep -A 5 "发布文章必须输出完整URL" /home/node/.openclaw/workspace/MEMORY.md
```

---
