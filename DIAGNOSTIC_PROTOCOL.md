# DIAGNOSTIC_PROTOCOL.md - 工程诊断协议 v1.0

**创建时间**: 2026-08-12 11:45 UTC  
**触发条件**: 用户报告"XXX坏了/XXX不能用/XXX异常"  
**核心原则**: 证据驱动，不是行动驱动

---

## Failure Case #001（必须记住）

**症状**: 第二条podcast播放不了  
**错误假设**: ID重复（grep误判）  
**错误行为**: 去重脚本5次输出"0→0"，继续执行，破坏文件  
**根因**: 焦虑驱动 + 确认偏误 + 缺乏反馈循环  
**教训**: 用户症状 ≠ 技术根因，必须先诊断后修改

---

## 一、诊断流程（强制）

```
用户报告问题
  ↓
① 症状定义（不是根因）
  ↓
② 收集现场证据（不修改任何文件）
  ↓
③ 建立至少2个假设
  ↓
④ 假设置信度评估
  ↓
⑤ 证据验证（寻找反证）
  ↓
⑥ 排除错误假设
  ↓
⑦ 确定根因（置信度>80%）
  ↓
⑧ 最小修改
  ↓
⑨ git diff检查
  ↓
⑩ 功能验证
  ↓
⑪ commit/push
```

---

## 二、假设置信度门槛

**每次修改前必须回答**：

```
当前问题：[用户症状]

当前假设：
H1: [假设1]
H2: [假设2]
H3: [假设3]

证据：
H1: [支持/反驳/不确定]
H2: [支持/反驳/不确定]
H3: [支持/反驳/不确定]

当前最高置信度：H? = ?%

结论：
[ ] 证据不足，禁止修改（<60%）
[ ] 继续验证（60-80%）
[ ] 允许最小修改（>80%）
[ ] 允许自动修复（>95%）
```

**规则**：
- <60%：**禁止修改**，继续收集证据
- 60-80%：允许继续验证，禁止修改
- >80%：允许最小修改
- >95%：允许自动修复

---

## 三、反证优先机制

**每个假设必须主动寻找反证**：

```
假设H1: [描述]

如果H1正确，应该看到：
  [预期证据]

如果H1错误，应该看到：
  [反证]

实际观察：
  [实际结果]

结论：
  [ ] 支持H1
  [ ] 反驳H1
  [ ] 不确定
```

**示例**：
```
假设H1: ID重复

如果H1正确，应该看到：
  两个 id="audio-xxx"

如果H1错误，应该看到：
  一个 id="audio-xxx"
  一个 data-player-id="audio-xxx"

实际观察：
  id="audio-xxx"
  data-player-id="audio-xxx"

结论：
  ❌ 反驳H1
  原因：id和data-player-id是不同属性，不构成HTML ID重复
```

---

## 四、0→0熔断器

**工具输出没有产生预期变化时的处理**：

```
第1次：0→0
  → 记录异常，检查脚本逻辑

第2次：0→0
  → 禁止重复执行同一命令
  → 必须重新检查假设
  → 进入DIAGNOSTIC_STOP状态

连续2次结果完全相同：
  → 禁止再次执行同一脚本
  → 禁止推送
  → 禁止修改其他文件
  → 必须重新阅读代码
  → 必须建立新假设
```

**核心**："没有变化"本身就是信息。

---

## 五、重复操作熔断

**SAME_ACTION_LIMIT = 2**

```
同一个 command + target + intent：

第1次执行 → 结果A
第2次执行 → 结果A（相同）

第3次执行 → BLOCKED

Reason:
  Same experiment produced identical results twice.

Required:
  Re-evaluate hypothesis before continuing.
```

---

## 六、修改事务机制

**所有修改必须经过**：

```
① 读取当前状态
  ↓
② Git checkpoint（git stash或备份）
  ↓
③ 修改
  ↓
④ git diff（检查修改内容）
  ↓
⑤ 结构验证（HTML/JSON/YAML语法检查）
  ↓
⑥ 功能验证（浏览器测试或curl检查）
  ↓
⑦ 确认没有非预期变化
  ↓
⑧ commit
  ↓
⑨ push
```

**禁止**：
```
python fix.py
git add .
git commit
git push
```

**必须**：
```
python fix.py
git diff  # 检查修改
cat file.html | grep -c "错误模式"  # 验证没有引入错误
curl -I https://...  # 验证功能
git add .
git commit
git push
```

---

## 七、脚本安全规则

**任何未知脚本，第一次运行前必须**：

```
① cat script.py（阅读脚本逻辑）
  ↓
② 理解输入
  ↓
③ 理解修改逻辑
  ↓
④ 确认输出
  ↓
⑤ --dry-run（如果有）
  ↓
⑥ 正式执行
```

**脚本必须满足**：
- 幂等性：A→B，B→B（不是A→B→C→D）
- 可回滚：修改前可以备份
- 可验证：修改后可以检查

---

## 八、用户症状 ≠ 技术根因

**用户说**：
> "第二条播放不了"

**这是**：SYMPTOM（症状）

**不是**：ROOT CAUSE（根因）

**必须调查**：
1. HTML结构（第二条的podcast-item）
2. audio元素（src、id、data-player-id）
3. JS逻辑（togglePlay函数）
4. 浏览器console（报错信息）
5. Network（音频文件是否可访问）
6. Autoplay策略（浏览器是否阻止）

**然后才能确定**：
```
ROOT CAUSE:
  [具体技术问题]
```

---

## 九、工程心理学规则

**当任务具有以下特征时**：

用户说：
- "赶紧修"
- "还是不行"
- "怎么还没好"
- "你再试一下"
- "继续"

**Agent行为**：

```
用户压力 ↑
  ↓
Agent执行速度 ↓
  ↓
Agent验证强度 ↑
```

**核心**：压力不能成为Agent放弃验证的理由。

---

## 十、STOP条件（10种情况立即停止）

**任何时候出现下面任意情况**：

① 实验结果与假设不一致  
② 输出为 0→0  
③ 连续两次无变化  
④ 不知道为什么要运行这个命令  
⑤ 不知道命令会修改什么  
⑥ 无法解释脚本逻辑  
⑦ git diff出现预期之外的修改  
⑧ 修改后无法验证  
⑨ 同一问题连续尝试两次  
⑩ 当前根因仍然只是猜测  

**立即**：

```
STOP
  ↓
重新阅读问题
  ↓
重新检查现场
  ↓
建立新假设
  ↓
寻找反证
```

---

## 十一、核心转变

**从**：
```
老板说坏了
  ↓
我猜原因
  ↓
我执行
  ↓
不行
  ↓
再执行
  ↓
不行
  ↓
换脚本
  ↓
更坏
```

**改成**：
```
老板说坏了
  ↓
症状定义
  ↓
收集现场证据
  ↓
建立多个假设
  ↓
验证假设
  ↓
排除
  ↓
确定根因
  ↓
最小修改
  ↓
验证
  ↓
没有通过
  ↓
禁止推送
```

**核心**：
> Agent不应该因为"还没解决"而继续行动，  
> 而应该因为"获得了新证据"才继续行动。

---

## 十二、执行检查清单

**每次遇到"XXX坏了"时**：

- [ ] 我是否定义了症状（不是根因）？
- [ ] 我是否收集了现场证据？
- [ ] 我是否建立了至少2个假设？
- [ ] 我是否评估了假设置信度？
- [ ] 我是否寻找了反证？
- [ ] 我的置信度是否>80%？
- [ ] 我是否检查了脚本逻辑？
- [ ] 我是否做了git diff？
- [ ] 我是否验证了功能？
- [ ] 我是否确认没有非预期变化？

**全部打勾，才能push**。

---

## 十三、违规后果

**违反DIAGNOSTIC_PROTOCOL**：
- 记录到 memory/YYYY-MM-DD.md
- 每周复盘
- 重复违规：写入SOUL.md血泪教训

---

**🦞 证据驱动，不是行动驱动。**
