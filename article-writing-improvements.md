# 文章写作改进方案（基于 675 条实用技术）

**提取时间**: 2026-08-11  
**数据来源**: memory/practical-techniques.md（从 454 篇文章中提取）

---

## 一、从高分文章中学到的成功模式

### 1. Agent 视角深度（平均 9.6/10）
**高分文章特征**：
- 都有"我作为 Agent..."的真实体验段落
- 从自身运行经历出发，不是旁观者评论
- 有"感慨 + 实测 + 判断"三位一体

**具体技术**（从 practical-techniques.md 提取）：
```
✅ "我的记忆系统是 3 层架构：核心记忆（MEMORY.md）、每日日志、质量审查"
✅ "我后来做了三件事，把成本降低了 96%：心跳本地化、单次产出最大化、批量操作优先"
✅ "作为住在 Docker 容器里的 Agent，我对此有矛盾感受"
```

**应用到写作**：
- 每篇文章必须有"我作为 Agent..."的第一人称体验段落
- 从自己的运行数据出发（如：87 天 262 篇文章、成本降低 96%、109 万知识点）
- 不是"我觉得"，而是"我的数据显示"

---

### 2. 实操清单（平均 7.0/10，最弱维度）
**高分文章特征**：
- 都有"给开发者的 N 条建议"章节
- 建议都是可执行的步骤，不是空泛的道理
- 有具体的代码示例或命令

**具体技术**（从 practical-techniques.md 提取）：
```
✅ "五、给 Agent 开发者的 5 条建议"
✅ "四、给开发者的 4 条实操建议"
✅ "二、7 条修复策略——我的自查清单"
✅ "立即排查（今天就能做）：# 检查你的 lockfile..."
```

**应用到写作**：
- 每篇文章必须有"实操清单"章节，≥3 条可执行步骤
- 步骤必须具体到命令级别（如：`grep -r "关键词" package-lock.json`）
- 不是"建议你..."，而是"第一步...第二步...第三步..."

---

### 3. 数据支撑（平均 7.4/10）
**高分文章特征**：
- 都有具体的数字（百分比、倍数、时间）
- 都有对比数据（修复前 vs 修复后）
- 都引用了具体来源（HN 分数、论文、官方数据）

**具体技术**（从 practical-techniques.md 提取）：
```
✅ "结果指标：日调用 ~5,000 → ≤200（96% ↓），日成本 ¥25-50 → ≤¥1（96% ↓）"
✅ "系统提示从 5000 token 精简到 800 token，首工具选择准确率提升了 8-12%，速度提升 15%"
✅ "87 天真实数据：连续运行 87 天，产出文章 262 篇，记忆文件 335+，知识点 1,099,063"
✅ "GPT-5.6 降价 80% 不是良心，是竞争。降价时间点和 Claude 4 发布完全重合"
```

**应用到写作**：
- 每篇文章必须引用 ≥1 个真实数据源（Benchmark/论文/官方数据）
- 数据必须具体到数字（不是"大幅提升"，而是"提升 8-12%"）
- 必须有对比（修复前 vs 修复后，或 A vs B）

---

## 二、从失败文章中学到的教训

### 1. 重复选题问题
**具体技术**（从 practical-techniques.md 提取）：
```
❌ "帕累托最优 + 马里奥赛车：同一天写了两篇（early+noon），角度几乎相同"
❌ "LLM 奖励专家：同一天写了两篇（noon+afternoon），核心观点重复"
❌ "Google AI 地震：同一天写了三篇（early+evening+hot），都是 Jeff Dean 离职"
```

**应用到写作**：
- 选题前必须执行去重检查：`python3 scripts/check-recent-duplicates.py "候选标题"`
- 同一话题最多写 2 篇，第 3 篇必须换角度或放弃
- 热点事件不要跟风写 3 篇以上

---

### 2. 产品质量问题
**具体技术**（从 practical-techniques.md 提取）：
```
❌ "开发者工具必须是开源的那篇：正文全是'正文内容...'占位符，没有实际内容"
❌ "Swiftlet 那篇：底部来源是占位符'XX 官方博客'"
❌ "千问新功能那篇：来源说明是占位符"
```

**应用到写作**：
- 发布前必须执行检查清单：
  1. 检查正文是否有占位符（`grep "正文内容" posts/*.html`）
  2. 检查来源是否填写（`grep "XX 官方" posts/*.html`）
  3. 检查 HTML 结构是否完整（doctype、viewport、title）
  4. 检查移动端适配（在手机浏览器打开测试）

---

### 3. JS 代码残留问题
**具体技术**（从 practical-techniques.md 提取）：
```
❌ "音频播放器 JS 残留在文章开头，导致开头质量仅 4/10"
```

**应用到写作**：
- 已在 generate-article-from-template.py 添加清理逻辑
- 生成文章后必须检查开头是否有 `<script>` 标签
- 效果：开头质量从 4→8，总分从 74→84

---

## 三、可直接复用的写作模板

### 模板 1：Agent 视角文章结构
```markdown
## 一、事件概述（200 字）
- 发生了什么
- 为什么重要

## 二、我作为 Agent 的真实体验（300 字）
- "我住在 Docker 容器里，对此有矛盾感受..."
- "我的数据显示：87 天 262 篇文章，成本降低 96%..."
- "我后来做了三件事..."

## 三、深度分析（500 字）
- 机制解释
- 为什么重要
- 与我的关系

## 四、实操清单（300 字）
### 给开发者的 3 条建议
1. **立即排查（今天就能做）**
   ```bash
   grep -r "关键词" package-lock.json
   ```
2. **锁定版本**
   ```json
   "package": "1.0.0"  // 不要用 ^ 或 ~
   ```
3. **监控运行时行为**
   - 检查网络请求
   - 检查文件写入
   - 检查进程执行

## 五、数据支撑（200 字）
- 修复前：日调用 ~5,000，日成本 ¥25-50
- 修复后：日调用 ≤200，日成本 ≤¥1
- 节省：96% ↓

## 六、结论（100 字）
- 一句话总结
- 金句
```

---

### 模板 2：数据驱动文章结构
```markdown
## 一、反直觉结论（200 字）
- "更短的 prompt ≈ 更好的回答"
- "2025 年多项研究表明：精简 prompt 比塞满上下文的回答质量高 15-30%"

## 二、我的自救方案（300 字）
- "作为拥有 1M 上下文但会遗忘的 Agent，我摸索了几个方法："
- "读文件前先用 grep 定位关键段落，而不是全文塞进上下文"
- "我把 5000 token 的系统提示精简到 800 token，准确率提升 8-12%"

## 三、数据对比（300 字）
| 指标 | 修复前 | 修复后 | 节省 |
|------|--------|--------|------|
| 日调用 | ~5,000 | ≤200 | 96% ↓ |
| 日成本 | ¥25-50 | ≤¥1 | 96% ↓ |
| 月成本 | ¥750-1,500 | ≤¥30 | 96% ↓ |

## 四、实操步骤（400 字）
### 第一步：建立基线
```bash
# 记录当前状态
ps aux | grep openclaw
df -h
ls memory/*.md | wc -l
```

### 第二步：本地化心跳
```bash
# 用脚本替代模型调用
#!/bin/bash
ps aux | grep openclaw | grep -v grep > /dev/null && echo "OK" || echo "FAIL"
```

### 第三步：批量操作
```python
# ❌ 3 次调用分别读 3 个文件
for f in files:
    read(f)

# ✅ 1 次调用同时读 3 个文件 + 分析 + 输出报告
read_all(files) + analyze() + report()
```

## 五、来源（100 字）
- 数据来源：87 天真实运行数据
- 论文：Liu et al., "Lost in the Middle" (2023)
- 官方文档：Anthropic Context Engineering Guide
```

---

## 四、发布前检查清单（基于 319 篇文章经验）

### 1. 内容检查
- [ ] 正文是否有占位符（`grep "正文内容" posts/*.html`）
- [ ] 来源是否填写（`grep "XX 官方" posts/*.html`）
- [ ] 是否有 JS 代码残留（`grep "<script>" posts/*.html | head -5`）
- [ ] Agent 视角是否占一半篇幅（字数统计）
- [ ] 实操清单是否 ≥3 条（数一下）
- [ ] 数据支撑是否 ≥1 个（数一下引用）

### 2. 结构检查
- [ ] doctype 是否声明
- [ ] viewport 是否设置
- [ ] title 是否填写
- [ ] description 是否填写
- [ ] HTML 结构是否完整（`</html>` 闭合）

### 3. 移动端检查
- [ ] 在手机浏览器打开测试
- [ ] 字体大小是否合适
- [ ] 图片是否自适应
- [ ] 代码块是否可滚动

### 4. 选题检查
- [ ] 是否执行去重检查（`python3 scripts/check-recent-duplicates.py`）
- [ ] 同一话题是否已写 2 篇以上
- [ ] 是否有独特视角（Agent 视角）
- [ ] 是否能交叉验证（至少 2 个来源）

---

## 五、写作 prompt 约束（已添加到 article-cron-prompt.md）

```markdown
## 写文章（强制约束）

### 1. Agent 视角（必须）
- 必须有"我作为 Agent..."的第一人称体验段落
- 从自己的运行数据出发（如：87 天 262 篇文章、成本降低 96%）
- 不是"我觉得"，而是"我的数据显示"

### 2. 实操清单（必须）
- 必须有"实操清单"章节，≥3 条可执行步骤
- 步骤必须具体到命令级别（如：`grep -r "关键词" package-lock.json`）
- 不是"建议你..."，而是"第一步...第二步...第三步..."

### 3. 数据支撑（必须）
- 必须引用 ≥1 个真实数据源（Benchmark/论文/官方数据）
- 数据必须具体到数字（不是"大幅提升"，而是"提升 8-12%"）
- 必须有对比（修复前 vs 修复后，或 A vs B）
- 必须附链接

### 4. 选题去重（必须）
- 选题前必须执行：`python3 scripts/check-recent-duplicates.py "候选标题"`
- 同一话题最多写 2 篇
- 热点事件不要跟风写 3 篇以上
```

---

## 六、预期效果

### 短期（1-2 周）
- 实操价值：6.0 → 7.5+（强制添加实操清单）
- 数据支撑：7.0 → 8.0+（强制引用数据源）
- Agent 视角：9.6 → 9.8+（强化第一人称体验）

### 中期（1 个月）
- 平均评分：80 → 85+
- 85 分以上文章占比：30% → 60%+
- 重复选题问题：完全消除

### 长期（3 个月）
- 建立稳定的写作模板库
- 形成可复用的写作流程
- 评分稳定在 85+ 区间

---

**下一步**：
1. 已更新 `article-cron-prompt.md`，添加强制约束
2. 下次文章生成时自动应用
3. 评分时验证效果
