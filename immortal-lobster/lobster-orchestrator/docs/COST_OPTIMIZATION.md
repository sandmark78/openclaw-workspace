# 💰 成本优化指南

**版本**: V0.4.2  
**目标**: 减少 90% 模型调用，节省 90% 成本  
**每日上限**: 200 次 (已固化)

---

## 📊 当前问题

**3 小时统计**:
- 调用次数：2255 次
- 估算成本：¥0.5-1.0
- 平均频率：4.8 秒/次 ❌ **极度浪费**

**每日上限**: 200 次 (已固化)  
**超限动作**: 自动暂停 + 报警

**主要原因**:
1. ❌ 每次编辑文件都调用模型
2. ❌ 简单任务也调用模型
3. ❌ 心跳调用模型 (已优化为本地执行)
4. ❌ 单次操作内容太少

---

## 🚀 优化方案

### 方案 1: 心跳本地化 ✅

**优化前**: 每 30 分钟调用模型  
**优化后**: 每 30 分钟本地执行  
**节省**: 99% (48 次/天 → ~0.5 次/天)

```bash
# 本地心跳脚本
./scripts/heartbeat-local.sh

# 无异常时回复：HEARTBEAT_OK
# 有异常时才调用模型
```

### 方案 2: 批量操作 ✅

**原则**: 一次调用完成多个任务

```bash
# ❌ 浪费：多次调用
write file1.md "内容 1"  # 调用 1
write file2.md "内容 2"  # 调用 2
write file3.md "内容 3"  # 调用 3

# ✅ 优化：一次调用
# 在单次对话中完成所有写入
```

**实施**:
- 规划好所有要写的文件
- 一次性列出所有内容
- 模型一次响应完成

### 方案 3: 本地执行优先 ✅

**原则**: 能本地执行就不调用模型

```bash
# ❌ 浪费：让模型写脚本
# (等待模型响应，写入文件)

# ✅ 优化：直接写脚本
cat > script.sh << 'EOF'
#!/bin/bash
echo "Hello"
EOF
chmod +x script.sh
```

**适用场景**:
- ✅ 简单文件创建
- ✅ 配置修改
- ✅ 脚本编写
- ✅ 命令执行

### 方案 4: 减少上下文长度 ✅

**问题**: 每次都带完整上下文 (浪费 tokens)

**优化**: 只带必要上下文

```bash
# ❌ 浪费：带上所有文件
# (1M tokens 上下文)

# ✅ 优化：只带相关文件
# (10K tokens 上下文)
```

**注意**: 不切换模型，保持 qwen3.5-plus 质量！

### 方案 5: 创建脚本模板库 ✅

**原则**: 常用文件模板化，避免重复生成

```bash
# ✅ 使用模板
cp templates/config.yaml configs/

# ✅ 使用批量创建脚本
./scripts/bulk-create.sh myproject
```

**适用场景**:
- ✅ 配置文件
- ✅ 脚本框架
- ✅ 文档模板
- ✅ 测试用例

---

## 📈 优化效果

| 项目 | 优化前 | 优化后 | 节省 |
|------|--------|--------|------|
| **心跳调用** | 48 次/天 | ~0.5 次/天 | 99% ↓ |
| **文件编辑** | 50 次/天 | 10 次/天 | 80% ↓ |
| **简单任务** | 30 次/天 | 5 次/天 | 83% ↓ |
| **总调用** | 128 次/天 | **≤200 次/天** | 上限控制 |
| **成本** | ¥2-4/天 | **≤¥11.5/天** | 上限控制 |

**月度成本**:
- 优化前：¥60-120/月
- 优化后：≤¥345.6/月 (上限)
- **实际**: 预计¥50-100/月 (正常使用)

---

## 🔧 实施清单

### 立即实施 ✅

- [x] 心跳本地化 (每 30 分钟，不调用模型)
- [x] 批量文件操作
- [x] 本地执行优先
- [x] **每日上限 200 次 (已固化)**
- [x] **超限自动暂停 + 报警**

### 本周实施 ⏳

- [ ] 创建脚本模板库
- [ ] 优化 Cron 任务
- [ ] 预生成文档模板

---

## 📝 最佳实践

### ✅ 应该做的

1. **批量操作**
   ```bash
   # 一次对话完成多个任务
   # 1. 创建文件 A
   # 2. 创建文件 B
   # 3. 修改配置 C
   ```

2. **本地执行**
   ```bash
   # 简单文件直接创建
   cat > file.md << 'EOF'
   内容
   EOF
   ```

3. **减少心跳**
   ```bash
   # 每 30 分钟一次
   # 无异常只回复 HEARTBEAT_OK
   # 不调用模型
   ```

4. **使用缓存**
   ```bash
   # 常用配置保存为模板
   cp templates/config.yaml configs/
   ```

5. **监控调用**
   ```bash
   # 查看当前调用次数
   cat memory/.model_call_count_$(date +%Y-%m-%d)
   ```

### ❌ 不应该做的

1. **不要切换模型** (保持 qwen3.5-plus 质量)
2. **不要每次只写一个小文件**
3. **不要简单任务也调用模型**
4. **不要频繁心跳 (每次都调用模型)**
5. **不要每次都带完整上下文**

---

## 🦞 省钱口诀

```
能批量，不单次；
能本地，不模型；
能简单，不复杂；
能缓存，不生成。

每日上限 200 次，超限自动暂停！
```

---

## 📞 监控成本

### 每日检查

```bash
# 查看调用次数
cat memory/.model_call_count_$(date +%Y-%m-%d)

# 查看监控日志
tail -20 memory/model-usage.log
```

### 每周报告

```bash
# 生成周报
# 1. 总调用次数
# 2. 总成本估算
# 3. 优化建议
```

---

## 🚨 超限处理

### 80% 警告

```
触发：≥160 次
动作：记录日志 + 警告通知
状态：继续监控
```

### 100% 暂停

```
触发：≥200 次
动作:
  1. 立即暂停模型调用
  2. 创建警报文件
  3. 发送警报通知
  4. 等待用户确认
```

### 解除暂停

```bash
# 1. 查看警报
cat memory/.model_alert

# 2. 确认继续
rm memory/.model_calls_paused

# 3. 清除警报
rm memory/.model_alert
```

---

## 📊 成本对比

### 优化前 (3 小时 2255 次)

- 心跳：~24 次
- 开发：~2200 次
- 其他：~31 次
- **总成本**: ¥0.5-1.0/3 小时

### 优化后 (预计 3 小时)

- 心跳：~0 次 (本地执行)
- 开发：~100 次 (批量操作)
- 其他：~5 次
- **总成本**: ¥0.05-0.1/3 小时
- **节省**: 90-95% ↓

---

**🦞 每一分钱都要花在刀刃上！**
